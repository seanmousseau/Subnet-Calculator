# Deny all direct HTTP access to files in this directory.
# Templates are included server-side via require — never via URL.
Require all denied

<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteRule .* - [F,L]
</IfModule>
