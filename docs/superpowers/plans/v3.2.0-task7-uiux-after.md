# Task 7 (#348) — ui-ux-pro-max AFTER consult

> Skill output produced via `python3 ~/.claude/skills/ui-ux-pro-max/scripts/search.py`
> against domain `ux` (queries from BEFORE pass plus the cross-check below) and
> synthesised against the implementation diff (~165 ins / ~5 del across 4 files).

## Verdict: **SHIP** (no must-fix blockers)

The implementation clears every Critical / High-severity rule the skill flags
for an admin secret-handling surface. Six concerns reviewed; none rise to
must-fix.

## Per-concern verdicts

| # | Concern | Verdict | Notes |
|---|---|---|---|
| A | Copy uses `textContent` not rendered HTML | **SHIP** | `htmlspecialchars` encodes for HTML output; the DOM round-trip via `textContent` decodes back to the original token bytes. The Playwright test asserts exact-match against the rendered text — round-trip integrity is verified, not assumed. |
| B | Dismiss removes from DOM (not `display: none`) | **SHIP** | Functionally equivalent to auto-dismiss on next nav. DOM removal is the simpler implementation and avoids leaving a hidden secret in the document tree (defence-in-depth: less for a future XSS to scrape). |
| C | Empty-state links `target="_blank"` | **SHIP** | Matches the project's footer doc-link pattern (`test_docs_footer_link`). `rel="noopener"` is set, so no reverse-tabnabbing risk. Consistency-with-project beats individual-preference here. |
| D | New `.splitter-btn-ghost` variant | **SHIP** | Visual-hierarchy decision is sound — Copy is the primary action, Got-it is dismiss/secondary. Token-driven (`--color-text-muted` / `--color-border` / `--color-accent`). Light + dark both work. Accessibility-wise it's still a real `<button>` with focus ring inheritance. |
| E | `document.execCommand('copy')` fallback | **SHIP** | The deprecation warning is informational; the API still works in every shipping browser. Project pattern (CLAUDE.md notes the iframe fallback uses the same approach). Modern fast path (`navigator.clipboard.writeText`) covers same-origin admin use; fallback is defence-in-depth for any future iframe embed. |
| F | Empty-state test skips when drain token unset | **SHIP** | This is the right ergonomic for a Playwright-fixture test that depends on env state outside the test runner's control. Fail-loud would block any developer running tests without the docker rig. Status reporter still emits the "skipped" line so it's visible in CI output. |

## Heuristic table

| Heuristic | Severity | Status |
|---|---|---|
| Confirmation Messages (severity Medium) | — | **Pass** — Copy → "Copied!" 1.5s text swap. |
| Empty States (severity Medium) | — | **Pass** — multi-paragraph nudge + 2 doc links + `<strong>` lede. |
| Touch Target Size (severity Critical) | — | **Pass** — buttons inherit `.splitter-btn` 36+ px. |
| Cursor Pointer (severity Critical) | — | **Pass** — real `<button>` elements. |
| Visible Focus States (severity Critical) | — | **Pass** — buttons inherit project focus rings; `.splitter-btn-ghost:focus-visible` rule explicit. |
| Form Control Labels (severity Critical) | — | N/A — no new form inputs. Buttons have visible text labels. |
| Color Contrast (4.5:1) | Critical | **Pass** — `--color-accent` on `--color-surface-alt` + `--color-text-muted` on `--color-bg` both AA-cleared by v2.9.0 audit. |
| `prefers-reduced-motion` | Medium | N/A — no animations beyond a 1.5s text swap (not motion). |
| Color is not the only indicator | High | **Pass** — Copy → "Copied!" text swap is the indicator, not just colour. |
| Z-Index Management | High | N/A — no fixed elements. |

## Findings classified

### Must-fix

_None._

### Recommended (apply during implementation)

_All previously called out are addressed by the diff:_

- ✅ Copy button shows "Copied!" then reverts after 1.5s.
- ✅ Dismiss button labelled "Got it" matching issue body.
- ✅ External-link `rel="noopener"` set.
- ✅ Panel keeps `role="alert"` so screen readers announce the new key.

### Nice-to-have (deferred)

1. **Auto-focus the Copy button** when the post-mint panel renders. Would let
   keyboard users hit Enter immediately. Trivial JS addition; deferred to
   keep the diff small. Re-evaluate if operators report it.
2. **Toast / pill confirmation** instead of inline text swap. Project hasn't
   standardised on a toast pattern; rolling out for one button creates
   inconsistency.
3. **Selecting the token text on click** of the `<code>` block. `user-select:
   all` is already on `.api-key-display` (CSS line 2484), so a triple-click
   already grabs the whole token. Marginal value-add.

## Net-new issues spotted in the diff

_None._

## Cross-check against Pre-Delivery Checklist

| Section | Status |
|---|---|
| No emoji icons | Pass — text-only labels. |
| Hover states without layout shift | Pass — `.splitter-btn-ghost:hover` only changes `color` + `border-color`. |
| `cursor: pointer` on clickable | Pass — real `<button>` elements. |
| Smooth transitions | Pass — `.splitter-btn-ghost` transitions inherit project defaults. |
| Light/dark contrast | Pass — token-driven; both themes verified by existing v2.9.0 typography test. |
| Floating-element spacing | N/A — no fixed elements. |
| Form inputs labeled | N/A — no new inputs. |
| `prefers-reduced-motion` | N/A. |
| Touch-target size | Pass. |
| Empty state | Pass — multi-paragraph nudge + 2 actionable links. |
| Confirmation feedback | Pass — Copy → "Copied!". |

## QA gates (verified at PR open)

- `php -l` on `admin/keys.php` — OK.
- `phpstan analyse --no-progress --memory-limit=512M` — 0 errors.
- `vendor/bin/phpunit` — 363 tests / 862 assertions (unchanged — UI polish only).
- `vendor/bin/phpcs --standard=PSR12 includes/ api/` + `--standard=.phpcs-admin.xml admin/` — 0 errors.
- `npm run lint:js` — 0 errors / 10 pre-existing warnings.
- `npm run lint:css` — clean.
- `make test-docker` — **1027 / 1027** Playwright assertions (was 1018; +9 across 3 new groups).
- `semgrep --config=.semgrep/rules.yml --config p/php …` — 0 findings.
