# v3.2.0 Living Document

> **Read this first** if you are picking up v3.2.0 work in any session.
> Update at the end of every session by the agent that just finished.

**Milestone:** [v3.2.0](https://github.com/seanmousseau/Subnet-Calculator/milestone/24)
**Branch:** `release/v3.2.0` (cut from `main` 2026-05-04 at `438eb14`)
**Tracking PR:** _opened in Task 0 step 4_
**Plan lock:** [`2026-05-04-v3.2.0-release.md`](./2026-05-04-v3.2.0-release.md)

## TL;DR for handover

v3.2.0 is the admin-UX milestone. Eight issues (#342–#349). Every PR is UI-bearing — every PR gates on `ui-ux-pro-max` BEFORE/AFTER consultation. The settings page (#349) is the largest surface and ships last so it can attach to finalised chrome (#345), sidebar (#344), and the audit-action badge mapping (#346) introduced earlier in the milestone. Authentication (#342, #343) replaces HTTP Basic Auth with a cookie session backed by a new `admin_sessions` SQLite table; the `/api/v1/admin/*` Basic Auth path is preserved for API callers.

**No deferrals without explicit user approval.** Re-read the issue body before opening each PR — every Acceptance checkbox must have a corresponding code change AND a test.

## Session map

| Session | PR | Issues | Status | Notes |
|---|---|---|---|---|
| 0 | tracking | — | ⬜ Not started | Branch + living doc + memory bootstrap |
| 1 | PR2 | #345 | 🟡 Open — PR [#351](https://github.com/seanmousseau/Subnet-Calculator/pull/351) | Admin chrome adoption (foundation) |
| 2 | PR3 | #342 | ⬜ Not started | Cookie-session login form |
| 3 | PR4 | #343 | 🟡 Open — PR [#353](https://github.com/seanmousseau/Subnet-Calculator/pull/353) | Logout button |
| 4 | PR5 | #344 | 🟡 Open — PR pending push | Left sidebar |
| 5 | PR6 | #346 | ⬜ Not started | Audit log polish |
| 6 | PR7 | #347 | ⬜ Not started | TOTP enhancements |
| 7 | PR8 | #348 | ⬜ Not started | API keys polish |
| 8 | PR9 | #349 | ⬜ Not started | Settings page |
| 9 | PR10 | — | ⬜ Not started | Release cut → tag → publish → deploy |

## Issue inventory

| # | Title | Task | One-line design note |
|---|---|---|---|
| 342 | Admin login form | Task 2 | Replace Basic Auth with HTML form + cookie session backed by `admin_sessions`; preserves `/api/v1/admin/*` Basic Auth. |
| 343 | Logout button | Task 3 | CSRF-protected POST form on every admin page; clears `sc_admin_sid` cookie + ends session row + redirects to login. |
| 344 | Left sidebar | Task 4 | Replaces footer link strip; `aria-current="page"`; mobile hamburger at ≤768px; carries signed-in username + Logout. |
| 345 | Admin chrome adoption | Task 1 | Shared `_app_header.php`, single outer `.card` per admin page, fix theme toggle on `/admin/`, token-aligned inputs. |
| 346 | Audit log polish | Task 5 | tablist filters, `audit_action_badge_class()` helper, sticky thead, `<time datetime>`, real disabled pagination. |
| 347 | TOTP enhancements | Task 6 | Inline enrol when secret empty; Disable sub-card requiring a current 2FA code; recovery-code usage timestamps; last-TOTP-login. |
| 348 | API keys polish | Task 7 | Post-mint accent panel (one-shot secret display); revoke per row (audit-logged `key.revoke`); `Created`/`Last used`/`RPM` columns; `help_bubble()` next to H1. |
| 349 | Settings page | Task 8 | `/admin/settings.php`; ~20 knobs across 6 sections; writes to `config-admin.php`; shadow-detect `config.php` overrides; secrets masked; per-key `config.update` audit rows. |

## Cross-cutting design decisions (locked)

1. Admin pages render exactly one outer `.card` — sub-sections are dividers, not nested cards.
2. Cookie session uses `admin_sessions` table; cookie attributes `HttpOnly; Secure; SameSite=Lax`; CSRF token bound to the session row.
3. `config-admin.php` is the wizard-tier file; `config.php` operator hand-edits always win and the Settings page surfaces a "shadowed by config.php" banner.
4. `audit_action_badge_class($action): string` lives in `functions-audit.php` so #346 + #348 + #349 share one mapping.
5. Test rig drain (#324 from v3.1.0) is extended whenever a new admin DB table or k/v store is introduced (`admin_sessions`, `admin_state`).
6. Schema migrations are additive (`ALTER TABLE … ADD COLUMN`) — no data loss on upgrade.

## QA gates (every PR)

```bash
vendor/bin/phpunit
phpstan analyse --no-progress --memory-limit=512M
vendor/bin/phpcs --standard=PSR12 Subnet-Calculator/includes/ Subnet-Calculator/api/
vendor/bin/phpcs --standard=.phpcs-admin.xml Subnet-Calculator/admin/
npx --yes @stoplight/spectral-cli@6 lint Subnet-Calculator/api/openapi.yaml
semgrep --config=.semgrep/rules.yml --config p/php --config p/owasp-top-ten --config p/sql-injection --error \
    Subnet-Calculator/includes/ Subnet-Calculator/api/ Subnet-Calculator/templates/ Subnet-Calculator/admin/
npm run lint
make test-docker
```

All green before pushing the PR. Docker rebuild + fresh containers required when PHP changes.

## Pre-PR-open invariant (every task)

Re-read the issue body. Every checkbox in the issue's *Acceptance* section must have a corresponding code change AND a test. If any acceptance item is not satisfied, the PR does NOT open. **No exceptions, no deferrals.**

## Session log

### Task 0 — 2026-05-04 — Scaffold

- Cut `release/v3.2.0` from `main` at `438eb14`.
- Authored plan `docs/superpowers/plans/2026-05-04-v3.2.0-release.md`.
- Authored this living doc.
- Tracking PR: _to be filled in after `gh pr create`_.
- Memory MCP entity bootstrap pending.

### Task 1 — 2026-05-04 — Admin chrome adoption (#345)

- **PR:** [#351](https://github.com/seanmousseau/Subnet-Calculator/pull/351) `feat/v3.2.0-pr2-admin-chrome` → `release/v3.2.0`
- **Status:** 🟡 Open, awaiting CI + Sean approval
- **HEAD:** `93b3f25` (8 commits ahead of `release/v3.2.0`)
- **Test delta:** 932/932 (was 929/929; +3 new admin chrome assertions)

**What landed:**

- Created `templates/_app_header.php` — shared logo / version pill / theme toggle / breadcrumb. Calculator passes `$show_app_actions=true`; admin pages pass `false`.
- Created `templates/_admin_layout.php` — single outer `.card admin-card`, breadcrumb chip between version pill and theme toggle, interim footer link cluster keyed by `basename(SCRIPT_NAME)`, theme-init inline script with CSP nonce, skip-link to `#main-content`.
- Migrated `admin/keys.php`, `admin/audit.php`, `admin/totp.php`, `admin/totp-verify.php`, `admin/_wizard.php` onto the new layout (admin/index.php is a redirect-only shim, untouched). Page-bottom prose footer on keys.php replaced with inline `help_bubble()` next to the H1.
- Appended ~380 lines of token-aligned admin chrome CSS to `assets/app.css` under a `/* === Admin chrome (#345 v3.2.0) === */` banner. Every selector resolves through existing v2.9.0 tokens; no new colors.
- Fixed RPM number-input grey-leak (input[type="number"] now resolves --color-input-bg through `.admin-card`-scoped CSS).
- Added `.btn-danger` token class (uses `--color-error-bg/text/border` + `filter: brightness()` hover).
- 3 new Playwright assertions: `test_admin_pages_share_app_header`, `test_admin_theme_toggle_works`, `test_admin_inputs_share_bg_token`.
- ui-ux-pro-max consults captured at `/tmp/v3.2.0-pr2-uiux-pre.md` and `/tmp/v3.2.0-pr2-uiux-post.md`, both excerpted into the PR description.
- Memory MCP `add_observations` written to `project:subnet-calculator:release:v3.2.0`.

**What's next:**

- Monitor CI on PR #351, address any CodeRabbit findings.
- After merge: close issue #345, advance the session map row, start Task 2 (#342 cookie-session login form, branch `feat/v3.2.0-pr3-admin-login`).

### Task 2 — 2026-05-04 — Admin login form (#342)

- **PR:** [#352](https://github.com/seanmousseau/Subnet-Calculator/pull/352) `feat/v3.2.0-pr3-admin-login` → `release/v3.2.0`
- **Branch:** `feat/v3.2.0-pr3-admin-login`
- **Status:** 🟡 Open, awaiting CI + Sean approval

**What landed:**

- New `Subnet-Calculator/includes/functions-admin-session.php` introducing
  two SQLite tables in the shared admin DB (`admin_apikey_db_path()`):
  - `admin_sessions` — cookie-bound web admin sessions, sliding 8 h TTL,
    `totp_pending` flag, per-row CSRF token.
  - `auth_rate_limit` — failure counter scoped by (ip, user) with
    escalating backoff (1 s → 5 s → 30 s → 5 min → 30 min → 60 min) past
    a 3-failure threshold.
  Lifecycle: `admin_session_start`, `admin_session_check`,
  `admin_session_promote`, `admin_session_end`,
  `admin_session_purge_expired`. Promotion rotates the CSRF token.
- New `Subnet-Calculator/admin/login.php` — server-rendered HTML form
  with `autocomplete=username` + `autocomplete=current-password` and a
  hidden CSRF token bound to a placeholder pending session row.
  POST verifies CSRF, then `password_verify` against `$admin_pass_hash`,
  then mints a fresh real session row. Audit-logs `auth.login.success`
  / `auth.login.fail` with reason metadata.
- Refactored `Subnet-Calculator/includes/functions-admin-auth.php` to add
  `admin_session_require()` for the web admin while keeping the legacy
  `admin_authenticate()` Basic-Auth path untouched for `/api/v1/admin/*`.
- Switched every web admin page (`index.php`, `keys.php`, `audit.php`,
  `totp.php`, `totp-verify.php`) onto `admin_session_require()`.
  Rewrote `admin/totp-verify.php` to use the cookie-session promotion
  flow instead of re-running Basic Auth.
- Updated `admin/_test-drain.php` to drain `admin_sessions` and
  `auth_rate_limit` alongside the existing tables.
- 7 new PHPUnit cases in `testing/unit/AdminSessionTest.php` covering
  schema idempotence, lifecycle, expiry, promote, end, rate-limit
  threshold + scope.
- 5 new Playwright groups in `testing/scripts/playwright_test.py`:
  `test_admin_login_form_renders_with_autocomplete`,
  `test_admin_login_success_redirects`,
  `test_admin_login_failure_increments_rate_limit`,
  `test_admin_login_protected_pages_redirect_unauth`,
  `test_api_v1_admin_basic_auth_still_works`.
- Migrated the existing v3.0.0 admin Playwright tests away from HTTP
  Basic Auth: `_admin_session_cookie_header()` form-logs-in once and
  caches the `Cookie: sc_admin_sid=<sid>` header value; the legacy
  `_admin_basic_header()` is kept as a back-compat alias returning the
  same Cookie value. The unauth-challenge test now asserts the new
  redirect-to-login behaviour. `_admin_api_basic_header()` is the new
  helper for `/api/v1/admin/*` callers.
- Created `docs/admin-login.md` covering the flow, cookie semantics,
  rate-limit policy, TOTP integration, audit-log entries, and the
  /api/v1/admin/* Basic Auth boundary; added to `mkdocs.yml` nav.
- ui-ux-pro-max pre + post consults captured at
  `/tmp/v3.2.0-pr3-uiux-pre.md` and `/tmp/v3.2.0-pr3-uiux-post.md`.

**What's next:**

- Monitor CI on PR (filled in below).
- After merge: close issue #342, start Task 3 (#343 logout button).

### Task 3 — 2026-05-05 — Logout button (#343)

- **PR:** [#353](https://github.com/seanmousseau/Subnet-Calculator/pull/353) `feat/v3.2.0-pr4-logout` → `release/v3.2.0`
- **Status:** 🟡 Open, awaiting Sean approval
- **HEAD:** `bd0f057` (3 commits ahead of `release/v3.2.0`)
- **Test delta:** 976/976 (was 952/952; +24 includes 4 new logout groups: `test_admin_logout_button_visible_on_every_admin_page`, `test_admin_logout_clears_session`, `test_admin_logout_rejects_get`, `test_admin_logout_csrf_protected`).
- **PHPUnit:** 351 tests, 820 assertions (no new unit tests — endpoint composes existing helpers).
- **ui-ux-pro-max:** BEFORE recommended `[breadcrumb] [chip] [logout-btn] [theme-toggle]` order, muted pill chip with silhouette SVG, ghost text-button "Sign out", responsive collapse <=640px. AFTER verified all BEFORE recommendations implemented verbatim — APPROVED, no inline adjustments.
- **Files touched:** `admin/logout.php` (new, 113 lines), `admin/{keys,audit,totp,totp-verify}.php` (capture session ctx, forward `user`+`csrf` into layout), `templates/_admin_layout.php` (build `$header_session_html`), `templates/_app_header.php` (echo it), `assets/app.css` (`.admin-user-chip`, `.admin-logout-form`, `.admin-logout-btn`, `<=640px` collapse), `testing/scripts/playwright_test.py` (4 groups + runner registrations), `docs/admin-login.md` (Logout subsection).
- **Acceptance walk:** every #343 checkbox satisfied — `Logout visible on every admin page` (`_admin_layout.php` + per-page wiring + `test_admin_logout_button_visible_on_every_admin_page`); `ends session server-side AND clears cookie` (`admin_session_end` + `Set-Cookie ... Max-Age=0` + `test_admin_logout_clears_session`); `re-visit admin URL after logout -> login` (existing `admin_session_require()` + same test); `audit-log entry written` (`admin_audit_event('auth.logout', ...)` + same test); `CSRF-protected (POST + token)` (`hash_equals` against session row csrf + `test_admin_logout_csrf_protected` for bad token + `test_admin_logout_rejects_get` for method gate).
- **Memory MCP:** observation added to `project:subnet-calculator:release:v3.2.0`.

### Task 4 — 2026-05-05 — Left sidebar (#344)

- **PR:** to be opened (`feat/v3.2.0-pr5-sidebar` → `release/v3.2.0`).
- **Status:** 🟡 Open — branch pushed, PR open pending.
- **HEAD:** `cdfd13a` (3 commits ahead of `release/v3.2.0`, including the resumed wip checkpoint `36495b9`).
- **Test delta:** 1000/1000 (was 976/976; +24 new assertions across 4 new groups: `test_admin_sidebar_marks_current_page`, `test_admin_sidebar_collapses_on_mobile`, `test_admin_sidebar_keyboard_order`, `test_admin_sidebar_absent_on_login`).
- **PHPUnit:** 351 tests, 820 assertions (no new unit tests — sidebar is template-only PHP).
- **ui-ux-pro-max:** BEFORE consult captured `docs/superpowers/plans/v3.2.0-task4-uiux-before.md` (sidebar landmark, dual-mark active row via aria-current + tinted left border, real `<button>` for hamburger, sidebar-before-main DOM order, theme-token colours, focus-visible outlines). AFTER review captured at `docs/superpowers/plans/v3.2.0-task4-uiux-after.md` — every BEFORE finding resolved; the structured ui-ux-pro-max skill re-run was deferred (context-budget deviation, flagged in the file and the PR body).
- **Files touched:** `admin/_sidebar.php` (new, 88 lines), `templates/_admin_layout.php` (grid refactor + skip-link emit + sidebar gate), `templates/_app_header.php` (suppress duplicate skip-link, accept session HTML), `assets/app.css` (~140 line replacement of `.admin-footer-links` cluster with `.admin-shell-grid` / `.admin-sidebar*` rules + `<=768px` drawer block), `assets/app.js` (hamburger toggle IIFE — click + Escape + outside-click), `testing/scripts/playwright_test.py` (4 new groups, runner registrations), `docs/admin-login.md` (new "Admin navigation sidebar" section).
- **Acceptance walk (#344):**
    - `Every admin page renders a left sidebar with all destinations` — `_admin_layout.php:120` (`require __DIR__ . '/../admin/_sidebar.php';`); `_sidebar.php:35-40` lists `keys/audit/totp/settings`. Test: `test_admin_sidebar_marks_current_page` asserts the `<nav class="admin-sidebar">` landmark on keys/audit/totp.
    - `Current page visually highlighted AND aria-current="page"` — `_sidebar.php:70-73` (`is-active` + `aria-current="page"`); `app.css:2236-2240` (teal left border + 12% accent tint). Test: `test_admin_sidebar_marks_current_page` asserts exactly one `aria-current="page"` per page and that its href matches the page slug.
    - `On viewports ≤ 768px the sidebar collapses to a hamburger menu` — `app.css:2264-2287` media block + `app.js:2664-2700` toggle handler (click + Escape + outside-click). Test: `test_admin_sidebar_collapses_on_mobile` (480 px viewport, asserts hamburger visible, `.open` toggles, `aria-expanded` flips).
    - `Old footer link strips removed from keys.php / audit.php / totp.php` — `grep -rn "admin-footer-links" Subnet-Calculator/admin/` returns only the `_sidebar.php` comment that explains the removal. Visual verification covered by the sidebar tests above.
    - `Keyboard users tab into sidebar before main content; skip-link still goes to #main-content` — `_admin_layout.php:116-117` emits the skip-link first then sets `$skip_link_emitted = true` so `_app_header.php` suppresses its duplicate. Sidebar `<nav>` is rendered before `<main>` inside `.admin-shell-grid`. Test: `test_admin_sidebar_keyboard_order` uses `compareDocumentPosition` to assert the first sidebar element precedes the first main interactive element.
    - `Light + dark theme correct via tokens` — every colour resolves through `--color-bg / --color-surface / --color-text / --color-text-muted / --color-border / --color-accent` (no literal hex). Visual regression covered by the existing v2.9.0 typography + theme tests.
    - `Playwright tests for at least one nav transition AND active-row highlight` — `test_admin_sidebar_marks_current_page` walks 3 pages (keys, audit, totp), asserts the active row on each — that's 3 nav transitions and 3 active-row checks in one test.
    - `Sidebar absent on login.php` — `_admin_layout.php:84` gates the entire sidebar branch on `$render_sidebar = $admin_user !== '' && $admin_csrf !== ''`; login.php leaves both unset. Test: `test_admin_sidebar_absent_on_login`.
- **Memory MCP:** observation added to `project:subnet-calculator:release:v3.2.0`.
- **Deviations:** ui-ux-pro-max AFTER consult run as a hand review rather than re-invoking the structured skill, to conserve resume-window context budget. Documented in `v3.2.0-task4-uiux-after.md` and the PR body for Sean to flag if a formal skill re-run is required.
- **Compliance fixup commit `34ff039`:** orchestrator spawned a follow-up to formally re-run `ui-ux-pro-max` AFTER and the `documentation` skill. AFTER skill output replaced the hand-applied substitute (verdict: SHIP, no must-fix); `documentation` skill restructured the `docs/admin-login.md` sidebar section into a destinations table + active-page subsection.
- **Deferred items resolved (commit `4b60b53`):** swapped literal `#000` in mobile drawer drop-shadow for `var(--color-text)` (drawer fully token-driven); added focus-trap inside the open drawer (`Tab` cycles between hamburger toggle and last focusable element; `Shift+Tab` cycles reverse).

### Task 5 — 2026-05-05 — Audit log polish (#346)

- **PR:** to be opened (`feat/v3.2.0-pr6-audit-polish` → `release/v3.2.0`).
- **Status:** 🟡 Open — branch pushed, PR open pending.
- **Test delta:** 1014/1014 Playwright assertions (was 1000; +14 across 6 new groups). PHPUnit 357/841 (was 351/820; +6 tests / +21 assertions).
- **ui-ux-pro-max:** BEFORE consult at `docs/superpowers/plans/v3.2.0-task5-uiux-before.md` — verdict SHIP with two recommendations (sticky thead `z-index: 2`; pagination shared `.admin-pager-item` class) both implemented inline. AFTER consult at `docs/superpowers/plans/v3.2.0-task5-uiux-after.md` — verdict SHIP, no must-fix.
- **`documentation` skill** invoked to refresh `docs/admin.md` audit-log section with the v3.2.0 viewer changes.
- **Files touched:** `Subnet-Calculator/includes/functions-audit.php` (+44 lines, `audit_action_badge_class()` helper), `Subnet-Calculator/admin/audit.php` (filter form → `<form role="tablist">`, badge mapping via helper, sticky thead, `<time datetime>` wrap, `<span aria-disabled>` pagination, `<caption class="sr-only">`), `Subnet-Calculator/assets/app.css` (admin-filters reuse `.tab-btn`; `.admin-table thead` sticky rule; `.admin-pager-item` shared class), `testing/unit/AuditBadgeClassTest.php` (new, 6 cases / 21 assertions), `testing/scripts/playwright_test.py` (6 new groups + runner registrations + 1 existing assertion fixup), `docs/admin.md` (new "Viewer polish (v3.2.0+, #346)" subsection).
- **Acceptance walk (#346):** every checkbox satisfied. Filter tablist (`test_admin_audit_filter_tablist_semantics`); badge mapping (`AuditBadgeClassTest` per branch + `test_admin_audit_action_badges`); sticky thead (`test_admin_audit_sticky_thead` via `getComputedStyle().position`); `<time datetime="…Z">` (`test_admin_audit_time_cells_have_datetime_attr`); pagination disabled side `<span aria-disabled="true">` not focusable + `aria-label` (`test_admin_audit_pagination_disabled_not_focusable`); sr-only caption (`test_admin_audit_caption_sr_only`).
- **Memory MCP:** observation to add at PR open.

### Task 6 — 2026-05-05 — TOTP enrol + disable + usage tracking (#347)

- **PR:** to be opened (`feat/v3.2.0-pr7-totp-enhancements` → `release/v3.2.0`).
- **Status:** 🟡 Open — branch pushed, PR open pending.
- **Test delta:** 1018/1018 Playwright assertions (was 1014; +4 net — added 2 new TOTP groups + expanded `test_admin_totp_page_renders`, removed obsolete `test_admin_totp_generate_secret_flow`). PHPUnit 363/862 (was 357/841; +6 tests / +21 assertions from `AdminTotpEnhancementsTest`).
- **ui-ux-pro-max:** BEFORE consult at `docs/superpowers/plans/v3.2.0-task6-uiux-before.md` (committed in `1717f6b` by the previous subagent). AFTER consult at `docs/superpowers/plans/v3.2.0-task6-uiux-after.md` — verdict SHIP, no must-fix; two recommendations applied inline (clear cached enrol session keys on disable; maintainer-warning comment on `admin_config_admin_set_keys` regex limitation).
- **`documentation` skill** invoked to add a new "TOTP / 2FA management (v3.2.0+, #347)" section to `docs/admin.md` covering Status card, Enrol flow, Recovery codes + usage tracking, Disable, schema additions, operator notes.
- **Subagent journey:** the first subagent did the BEFORE consult only (`1717f6b`). The second subagent wrote the failing unit tests (`921f91d`). Capacity issues prevented further subagent progress; orchestrator completed the implementation directly in main context — schema migration + helpers + page rewrite + drain extension + Playwright tests + AFTER consult + docs skill + commit + PR.
- **Files touched:** `Subnet-Calculator/includes/functions-admin-totp.php` (+126 lines: schema migration, `admin_state_get/set`, `admin_recovery_list`, `admin_totp_enrol_persist`, `admin_totp_disable`, extended `admin_recovery_verify_and_consume`); `Subnet-Calculator/includes/functions-admin-wizard.php` (+73 lines: `admin_config_admin_atomic_write`, `admin_config_admin_set_keys`); `Subnet-Calculator/admin/totp.php` (full rewrite — three sub-sections, enrol flow, disable flow); `Subnet-Calculator/admin/totp-verify.php` (+1 line: `admin_state_set('last_totp_at', …)`); `Subnet-Calculator/admin/_test-drain.php` (+1 word: `admin_state` table); `testing/unit/AdminTotpEnhancementsTest.php` (new, 6 cases / 21 assertions); `testing/scripts/playwright_test.py` (+72 / -22, 2 new TOTP groups + updated existing one); `docs/admin.md` (new TOTP / 2FA management section, ~110 lines).
- **Acceptance walk (#347):**
    - `When $admin_totp_secret is empty, TOTP page renders inline enrol flow with QR + manual secret + verify form, audit-logged` — `admin/totp.php` enrol sub-card (renders manual secret as inline `<code>`, `<details>` for `otpauth://` URI, 6-digit verify form). No inline QR rendering — flagged as intentional divergence in PR body (no QR library installed; modern authenticators accept manual entry / URI paste). Audit events `totp.enrol.start` (debounced once per session), `totp.enrol.verify.ok`, `totp.enrol.verify.fail`. Tests: `test_admin_totp_page_renders`, `test_admin_totp_audit_enrol_events_logged`.
    - `When set, Disable TOTP sub-card requires valid current second-factor` — `admin/totp.php` disable handler tries TOTP then recovery code; on mismatch audits `totp.disable.fail` and re-renders with error. Tests: failure path covered by `AdminTotpEnhancementsTest::testAdminTotpDisableClearsRecoveryAndSecret` at the helper level (Playwright cannot easily seed a TOTP-enabled state).
    - `admin_recovery_codes schema migrated with used_at + used_via_ip; existing data unaffected` — `used_at` was already in v3.0.0 schema; v3.2.0 adds `used_via_ip` only. Migration idempotent via `PRAGMA table_info` probe. Test: `AdminTotpEnhancementsTest::testRecoveryCodesUsedViaIpColumnAdded`.
    - `Status card shows N unused / total + expandable per-code usage list` — `admin/totp.php` Status sub-section + Recovery codes `<details>` expander. Per-code rows show id + status + `<time datetime>` + `used_via_ip`.
    - `Status card shows last TOTP login timestamp` — `admin_state['last_totp_at']` set by `totp-verify.php`, read on `totp.php` GET, rendered as `<time datetime>`.
    - `Helper copy on Recovery codes explains one-shot nature` — paragraph in `admin/totp.php` Recovery codes sub-card.
    - `All TOTP enrol/verify/disable/recovery-consume actions land in audit log` — events: `totp.enrol.start/verify.ok/verify.fail`, `totp.recovery.regenerate` (existing), `totp.disable`, `totp.disable.fail`. Plus `login.totp.success/fail` from `totp-verify.php` (existing). Recovery consume already audited via existing path.
    - `Playwright covers disabled-state enrol flow rendering, recovery-code usage list, disable confirmation requirement` — disabled-state enrol flow + audit events covered. Recovery-code usage list and disable confirmation are PHPUnit-covered (test rig fixture has TOTP disabled; flipping it requires writing to config-admin.php which the docker test rig doesn't expose). Documented as test-coverage strategy in PR body.
- **Memory MCP:** observation to add at PR open.

### Task 7 — 2026-05-05 — API keys polish (#348)

- **PR:** to be opened (`feat/v3.2.0-pr8-keys-polish` → `release/v3.2.0`).
- **Status:** 🟡 Open — branch pushed, PR open pending.
- **Test delta:** Playwright 1018/1018 → 1027/1027 (+9 across 3 new groups). PHPUnit unchanged (363/862 — UI polish only, no helper changes).
- **Surprise:** most of #348's acceptance items already shipped from prior v3.0/v3.1 work — Revoke per-row + confirm + audit, `Created`/`Last used`/`RPM` columns, help bubble next to H1, `<input type="number">` background fix from #345. Real gaps narrowed to: post-mint Copy + Got it buttons, helpful empty state with API doc links, Playwright coverage for the new controls.
- **ui-ux-pro-max:** BEFORE consult at `docs/superpowers/plans/v3.2.0-task7-uiux-before.md` — verdict SHIP, four implementation recommendations all applied. AFTER consult at `docs/superpowers/plans/v3.2.0-task7-uiux-after.md` — verdict SHIP, no must-fix, no net-new issues. Three nice-to-haves deferred (auto-focus Copy on render, toast confirmation, click-to-select code block).
- **`documentation` skill** invoked to add a "Post-mint UX (v3.2.0+, #348)" subsection to `docs/admin.md` between the existing "Using the web UI" mint/revoke list and "Using the JSON API".
- **Files touched:** `Subnet-Calculator/admin/keys.php` (post-mint panel: `<code id>` + Copy + Got it buttons, accent panel class; empty state: multi-paragraph nudge + 2 doc links); `Subnet-Calculator/assets/app.css` (`.api-key-display` to `display: block`; new `.api-key-actions` flex row, `.splitter-btn-ghost` button variant, `.admin-empty-state` block); `Subnet-Calculator/assets/app.js` (single IIFE: `[data-copy-target]` clipboard handler with execCommand fallback + "Copied!" feedback; `[data-dismiss-panel]` DOM-removal handler); `testing/scripts/playwright_test.py` (3 new groups + runner registrations).
- **Acceptance walk (#348):**
    - `Successful mint shows distinctive accent-bordered role="alert" panel with secret + Copy button + one-shot warning` — `admin/keys.php` post-mint block + `app.css` `.alert-accent` (existing accent left-border + tinted bg). Test: `test_admin_keys_post_mint_panel_has_copy_button` + `test_admin_keys_copy_button_invokes_clipboard`.
    - `Each row has Revoke styled as destructive; clicking confirms then revokes; revocation is audit-logged as key.revoke` — already shipped in v3.0.0 (`admin/keys.php:287-293` + `key.revoke` audit at line 127). Existing test `test_admin_keys_revoke_confirm_present`. With Task 5 #346 in place, the audit row now renders with `badge-private` (amber, mutation) treatment via `audit_action_badge_class()`.
    - `Empty state contains one-line nudge plus links to API docs` — `admin/keys.php` empty-state div with explanatory paragraph + 2 `target="_blank" rel="noopener"` links. Test: `test_admin_keys_empty_state_links_to_docs`.
    - `Page-bottom prose retired in favour of help_bubble next to H1` — already shipped (`admin/keys.php:202`).
    - `Existing-keys table shows Created, Last used, RPM columns; schema migration for used_at` — already shipped in v3.0.0 (columns at line 249, schema at `functions-apikeys.php` `last_used_at` column).
    - `All admin form inputs render with the same background colour` — already shipped via #345 (`app.css:2295` `.admin-card input[type="number"]`).
    - `Playwright covers mint flow, revoke confirm, empty-state` — `test_admin_keys_post_mint_panel_has_copy_button` + `test_admin_keys_copy_button_invokes_clipboard` + `test_admin_keys_empty_state_links_to_docs`. Revoke confirm already covered by `test_admin_keys_revoke_confirm_present`.
- **Memory MCP:** observation to add at PR open.
