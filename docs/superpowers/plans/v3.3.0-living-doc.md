# v3.3.0 Living Document

> **Read this first** if you are picking up v3.3.0 work in any session.
> Update at the end of every session by the agent that just finished.

**Milestone:** [v3.3.0](https://github.com/seanmousseau/Subnet-Calculator/milestone/25)
**Branch:** `release/v3.3.0` (cut from `main` 2026-05-06 at `3843aef`)
**Tracking PR:** [#369](https://github.com/seanmousseau/Subnet-Calculator/pull/369)
**Plan lock:** [`2026-05-06-v3.3.0-release.md`](./2026-05-06-v3.3.0-release.md)

## TL;DR for handover

v3.3.0 is the **first** of four IPv6-themed minor releases. It ships five new
tools — `range6`, `supernet6`, `zone-id`, `derive`, `slaac-privacy` — as
**drawer entries on the existing IPv6 tab**. **No new top-level tabs.** No
new design tokens. GMP throughout for IPv6 arithmetic.

Sequencing intent: parity ports first (range6, supernet6 mirror their v4
siblings), then the IPv6-native tools (zone-id, derive, slaac-privacy). The
plan locks each tool to its own PR off `release/v3.3.0`; only the release-cut
PR (PR7) merges `release/v3.3.0` into `main`.

**No deferrals without explicit user approval.** Every Acceptance checkbox in
each issue must have a corresponding code change AND a test before its PR
opens.

## Session map

| Session | PR | Issues | Status | Notes |
|---|---|---|---|---|
| 0 | tracking | — | ⬜ Not started | Branch + living doc + memory bootstrap |
| 1 | PR2 | #364 | ⬜ Not started | `range6→CIDR` + IPv4 cap backport |
| 2 | PR3 | #365 | ⬜ Not started | `supernet6` / `summarise6` |
| 3 | PR4 | #366 | ⬜ Not started | Zone-ID parser |
| 4 | PR5 | #367 | 🟡 In review (PR #373) | `derive` (combined MAC tool) — backend `9468bde`, UI/Playwright/docs `28f2396` |
| 5 | PR6 | #368 | ⬜ Not started | SLAAC privacy generator |
| 6 | PR7 | — | ⬜ Not started | Release cut → tag → publish → deploy |

## Issue inventory

| # | Title | Task | One-line design note |
|---|---|---|---|
| 364 | `range6→CIDR` (IPv6 range to minimal CIDR list) | Task 1 | Mirror IPv4 `range_to_cidrs()` using GMP; reuse `sc_run_*` shareable URL pattern; backport host-count cap to v4 path. |
| 365 | `supernet6` / `summarise6` | Task 2 | IPv6 equivalents of `supernet_find()` / `summarise_cidrs()`; longest-common-prefix on GMP integers; canonical compressed output. |
| 366 | Zone-ID parser | Task 3 | Accept `fe80::1%eth0` form; split + validate scope-id per RFC 4007; route into existing IPv6 calculator. |
| 367 | `derive` (combined MAC tool) | Task 4 | One MAC input → EUI-64 interface ID + link-local + solicited-node multicast. RFC 4291 §2.5.1 vectors (U/L bit flip). |
| 368 | SLAAC privacy generator | Task 5 | RFC 8981 privacy address generator; optional 16-hex seed (advanced disclosure); unseeded path uses `random_bytes`. |

## Cross-cutting design decisions (locked)

1. **Drawer-only UI.** All five tools land as `data-tool="..."` drawer entries
   on the existing IPv6 tab — mirror existing markup in `templates/layout.php`.
2. **No new top-level tabs.**
3. **No new design tokens.** Consume existing `--color-*` only; teal accent
   `#06d6a0` and dark text on teal buttons (per v2.9.0 style guide).
4. **GMP throughout** for IPv6 arithmetic — never PHP signed int math on
   addresses or counts. Counts that overflow signed 64-bit print as the
   `"2^N"` string form already used by `calculate_subnet6()` and v2.11
   IPv6 VLSM.
5. **RFC vectors in tests.** `Derive6Test` uses RFC 4291 §2.5.1 EUI-64
   vectors (verify U/L bit flips from 0 to 1). `SlaacPrivacyTest` uses
   RFC 8981 §3.3 invariants — bit 6 of byte 8 must be cleared (`0x02`
   mask in PHP byte indexing).
6. **Shareable URL via `sc_run_*` helpers.** Each tool gets one
   `sc_run_<tool>` helper in `Subnet-Calculator/includes/request.php` that
   handles both POST submission and GET hydration, populating the same
   template globals regardless of method. Same pattern as v2.11
   `sc_run_lookup` / `sc_run_diff`.
7. **`help_bubble()` on every input.** Inherited a11y (tabindex/role/aria
   from v2.5.0) means no extra wiring per bubble.
8. **No new design tokens, no new tabs.** Restated for emphasis — these are
   the two most-likely-to-erode invariants of the milestone.

## QA gates (every PR)

```bash
vendor/bin/phpunit
phpstan analyse --no-progress --memory-limit=512M
vendor/bin/phpcs --standard=PSR12 Subnet-Calculator/includes/ Subnet-Calculator/api/
vendor/bin/phpcs --standard=.phpcs-admin.xml Subnet-Calculator/admin/
npx --yes @stoplight/spectral-cli@6 lint Subnet-Calculator/api/openapi.yaml
semgrep --config=.semgrep/rules.yml --config p/php --config p/owasp-top-ten --config p/sql-injection --error \
    Subnet-Calculator/includes/ Subnet-Calculator/api/ Subnet-Calculator/templates/
npm run lint
make test-docker
```

All green before pushing the PR. Docker rebuild + fresh containers required
when PHP changes.

## Pre-PR-open invariant (every task)

Re-read the issue body. Every checkbox in the issue's *Acceptance* section
must have a corresponding code change AND a test. If any acceptance item is
not satisfied, the PR does NOT open. **No exceptions, no deferrals.**

The UI design-language gate (`ui-ux-pro-max` BEFORE and AFTER) and the
`documentation` skill gate are mandatory on every UI- or doc-bearing PR.
Document the consultant output verbatim in the PR description.

## Memory MCP

- **Entity:** `project:subnet-calculator:release:v3.3.0` (entityType `release`)
- **Relations:**
  - `project:subnet-calculator --has-release--> project:subnet-calculator:release:v3.3.0`
  - `project:subnet-calculator:release:v3.2.4 --predecessor-of--> project:subnet-calculator:release:v3.3.0`
- **Note:** the `project:subnet-calculator:release:v3.2.4` entity did not
  exist before this scaffold and was created as a stub (one observation)
  so the predecessor relation could be established. Backfill earlier
  release stubs (v3.2.1–v3.2.3) opportunistically.

## Session log

### Task 0 — 2026-05-06 — Scaffold

- Cut `release/v3.3.0` from `main` at `3843aef`.
- Plan lives on remote branch `origin/docs/v3.3.0-spec` (PR #363) and will
  land on `main` separately; not duplicated onto this branch.
- Authored this living doc.
- Seeded Memory MCP entity `project:subnet-calculator:release:v3.3.0` plus
  the two relations above. Created `release:v3.2.4` stub entity to anchor
  the predecessor edge (was missing from the graph).
- Tracking PR: [#369](https://github.com/seanmousseau/Subnet-Calculator/pull/369) (`release/v3.3.0` → `main`).

## PR3: supernet6 / summarise6

- **Branch:** `feature/v3.3.0-supernet6` → `release/v3.3.0`
- **Commit SHA:** `7dd0550`
- **Issue:** #365
- **Files:**
  - `Subnet-Calculator/includes/functions-supernet6.php` (new) — `supernet6_find()`, `summarise6_cidrs()` (GMP, strict types)
  - `Subnet-Calculator/api/v1/handlers/supernet6.php` (new) — `POST /api/v1/supernet6`
  - `Subnet-Calculator/api/v1/index.php` — router wire-up
  - `Subnet-Calculator/index.php` — bootstrap include
  - `Subnet-Calculator/api/openapi.yaml` — `/supernet6` path
  - `Subnet-Calculator/includes/request.php` — `sc_run_supernet6()` POST+GET helper
  - `Subnet-Calculator/templates/layout.php` — drawer trigger + panel
  - `testing/unit/Supernet6Test.php` (new) — 17 unit tests
  - `testing/unit/bootstrap.php` — register new helpers
  - `testing/scripts/playwright_test.py` — new test group
  - `testing/scripts/capture_supernet6_snapshots.py` (new) — one-off snapshot helper
  - `docs/ipv6.md` — Supernet & Summarise section
  - `CHANGELOG.md` — Unreleased/Added entry
- **Test deltas:** PHPUnit 400 → **417** (+17 tests, +36 assertions). Playwright 1086 → **1102** (+16 assertions, +1 group).
- **Gate outcomes:**
  - PHPUnit: 417 / 981 ✓
  - PHPStan L9: clean ✓
  - PHPCS PSR-12: clean ✓
  - Spectral OpenAPI: 0 errors ✓
  - Semgrep: 0 findings ✓
  - npm run lint: 0 errors ✓
  - `make test-docker`: 1102/1102 passed ✓
- **Snapshots committed:**
  - `testing/snapshots/ipv6-supernet-closed.png`
  - `testing/snapshots/ipv6-supernet-open.png`
  - `testing/snapshots/ipv6-supernet-error.png`
  - `testing/snapshots/ipv6-range-closed.png` (bonus — deferred from PR #370)
  - `testing/snapshots/ipv6-range-open.png` (bonus)
  - `testing/snapshots/ipv6-range-error.png` (bonus)
- **Skill consults:** ui-ux-pro-max BEFORE + AFTER, documentation skill — captured verbatim in PR description.

## PR4: zone-ID parser

- **Branch:** `feature/v3.3.0-zone-id` → `release/v3.3.0`
- **Commit SHAs:** `341b6db` (backend), `85a8051` (UI + Playwright + docs)
- **Issue:** #366
- **Files:**
  - `Subnet-Calculator/includes/functions-derive6.php` (new) — `parse_zone_id()` (RFC 4007, GMP-backed link-local detection)
  - `Subnet-Calculator/api/v1/handlers/zone-id.php` (new) — `POST /api/v1/zone-id`
  - `Subnet-Calculator/api/v1/index.php` — router wire-up + meta
  - `Subnet-Calculator/index.php` — bootstrap include for derive6
  - `Subnet-Calculator/api/openapi.yaml` — `/zone-id` path
  - `Subnet-Calculator/includes/request.php` — `sc_run_zoneid()` POST+GET helper
  - `Subnet-Calculator/templates/layout.php` — drawer trigger + panel
  - `Subnet-Calculator/assets/app.css` — `.zoneid-result` styles (no new tokens)
  - `Dockerfile` — disable `$api_rate_limit_rpm` in test rig (prevents cumulative-API-cap flakes)
  - `testing/unit/ZoneIdTest.php` (new) — backend unit tests
  - `testing/unit/bootstrap.php` — register helper
  - `testing/scripts/playwright_test.py` — `test_ipv6_zoneid_ui`, `test_ipv6_zoneid_shareable_url`, `test_api_zoneid`
  - `testing/scripts/capture_zoneid_snapshots.py` (new) — snapshot helper
  - `testing/snapshots/ipv6-zoneid-{closed,open,success,warning,error}.png` (new)
  - `docs/ipv6-zone-id.md` (new) + `mkdocs.yml` nav
  - `CHANGELOG.md` — Unreleased/Added entry
- **Test deltas:** PHPUnit 417 → **430** (+13 tests, +23 assertions). Playwright 1117 → **1146** (+29 assertions, +3 groups).
- **Gate outcomes:**
  - PHPUnit: 430 / 1004 ✓
  - PHPStan L9: clean ✓
  - PHPCS PSR-12: clean ✓
  - Spectral OpenAPI: 0 errors ✓
  - Semgrep: 0 findings ✓
  - npm run lint: 0 errors ✓
  - `make test-docker`: 1146/1146 passed ✓
- **Snapshots committed:**
  - `testing/snapshots/ipv6-zoneid-closed.png`
  - `testing/snapshots/ipv6-zoneid-open.png`
  - `testing/snapshots/ipv6-zoneid-success.png`
  - `testing/snapshots/ipv6-zoneid-warning.png`
  - `testing/snapshots/ipv6-zoneid-error.png`
- **Skill consults:** ui-ux-pro-max BEFORE + AFTER (definition-list semantics for the result rows; reuse existing `--color-*` tokens; mirror `.overlap-panel` shell from supernet6/range6).

## PR5: derive (MAC → IPv6 forms)

- **Branch:** `feature/v3.3.0-derive` → `release/v3.3.0`
- **Commit SHAs:** `9468bde` (backend), `28f2396` (UI + Playwright + docs)
- **PR:** #373
- **Issue:** #366 (combined MAC-derivation tool)
- **Files:**
  - `Subnet-Calculator/includes/functions-derive6.php` — extended with `mac_to_eui64()`, `mac_to_link_local()`, `unicast_to_solicited_node()`, `derive_from_mac()`, plus a private MAC normaliser
  - `Subnet-Calculator/api/v1/handlers/derive.php` (new) — `POST /api/v1/derive`
  - `Subnet-Calculator/api/v1/index.php` — `/derive` route + meta enum
  - `Subnet-Calculator/api/openapi.yaml` — `/derive` path
  - `Subnet-Calculator/includes/request.php` — `sc_run_derive()` POST+GET helper + 8 template globals + `$is_derive` form-protection bypass
  - `Subnet-Calculator/templates/layout.php` — drawer trigger + 4-row result panel with per-row copy buttons
  - `Subnet-Calculator/assets/app.css` — `.derive-result` styles (no new tokens)
  - `testing/unit/Derive6Test.php` (new) — 24 backend tests covering RFC 4291 vector + multicast warning + invalid input
  - `testing/scripts/playwright_test.py` — `test_ipv6_derive_ui`, `test_ipv6_derive_copy_buttons`, `test_ipv6_derive_shareable_url`, `test_api_derive`
  - `testing/scripts/capture_derive_snapshots.py` (new) — snapshot helper
  - `testing/snapshots/ipv6-derive-{closed,open,success,warning,error}.png` (new)
  - `docs/ipv6-derive.md` (new) + `mkdocs.yml` nav
  - `CHANGELOG.md` — Unreleased/Added entry
- **Test deltas:** PHPUnit 430 → **454** (+24 tests, +40 assertions). Playwright 1146 → **1183** (+37 assertions, +4 groups).
- **Gate outcomes:**
  - PHPUnit: 454 / 1044 ✓
  - PHPStan L9: clean ✓
  - PHPCS PSR-12: clean ✓
  - Spectral OpenAPI: 0 errors ✓
  - Semgrep: 0 findings ✓
  - npm run lint: 0 errors ✓
  - `make test-docker`: 1183/1183 passed ✓
- **Snapshots committed:**
  - `testing/snapshots/ipv6-derive-closed.png`
  - `testing/snapshots/ipv6-derive-open.png`
  - `testing/snapshots/ipv6-derive-success.png`
  - `testing/snapshots/ipv6-derive-warning.png`
  - `testing/snapshots/ipv6-derive-error.png`
- **Skill consults:** ui-ux-pro-max BEFORE + AFTER. BEFORE: mirror `.zoneid-result` `<dl>` grid exactly, extend to four rows (MAC canonical + 3 outputs), inline `.subnet-copy` per output `<dd>`, reuse `.warning`/`.error` bands. AFTER: 9rem label column accommodates longer labels without crowding values; values wrap on narrow widths; no further polish needed.
- **Math correction:** spec line referenced EUI-64 = `0226:b9ff:fe7e:abcd` for input `00:24:b9:7e:ab:cd`; correct value (used in code, tests, and docs) is `0224:b9ff:fe7e:abcd` — first byte `0x00 ^ 0x02 = 0x02`, second byte `0x24` unchanged → first hextet `0224`. Spec/plan typo not corrected here; flagged for separate cleanup.
